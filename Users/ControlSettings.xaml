<UserControl x:Class="Users.ControlSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Users"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" x:Name="mainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border BorderThickness="0,0,0,0.8" BorderBrush="#FF666666">
            <Grid Background="#FFEFE7">
                <TextBlock  Foreground="#252525" Text="НАСТРОЙКИ" VerticalAlignment="Center" Margin="20,0,0,0"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Background="#FFF0F0F0" >
            
        </Grid>
    </Grid>
</UserControl>
